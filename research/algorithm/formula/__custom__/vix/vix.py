"""
Type: Option
Name: Day to Expire
Method: Hold X day until expire
"""
import numpy as np
import pandas as pd
from pandas.tseries.offsets import Day


def handle_data(df, df_all, df_thinkback):
    return df


def create_signal(df, df_all):
    return None
